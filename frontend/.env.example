# Copy to .env.local to point the dashboard at a running backend.
# Leave unset (the default) to replay the episode recordings bundled with the
# site - that is how the deployed Vercel build works.
VITE_API_BASE=http://127.0.0.1:8000
