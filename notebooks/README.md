# notebooks/

Exploratory analysis lives here (training curves from `data/logs`, reward
ablations, plots for the report).

Keep notebooks *read-only* with respect to `data/`: any number that ends up in
the report must be reproducible with `python scripts/run_experiments.py`, not
only inside a notebook cell.
